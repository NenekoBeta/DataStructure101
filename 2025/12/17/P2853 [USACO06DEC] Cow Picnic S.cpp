#include <iostream>
#include <vector>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int K, N, M;
    cin >> K >> N >> M;

    vector<int> cows(K);
    for (int i = 0; i < K; ++i) cin >> cows[i];

    vector<vector<int>> adj(N + 1);
    for (int i = 0; i < M; i++)
    {
        
    }
    
}
